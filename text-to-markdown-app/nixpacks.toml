# Configuração para servir arquivos estáticos
[variables]
NODE_ENV = "production"
PORT = "80"

[phases.install]
cmds = ["npm ci"]

[phases.build] 
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -l 80"